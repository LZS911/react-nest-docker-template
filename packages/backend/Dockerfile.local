FROM node:16-alpine
Run mkdir -p /svr/app
WORKDIR /svr/app
RUN yarn add -g @nestjs/cli
COPY package.json .
COPY yarn.lock .
RUN yarn
COPY . .

# Value set in .env file.
EXPOSE $BACKEND_PORT
CMD ["yarn", "start:debug"]